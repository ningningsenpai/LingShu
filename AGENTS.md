# AGENTS.md

本文件是灵枢仓库的项目级 AI 协作约定。所有在本仓库工作的 AI 代理都应遵守以下规则。

## 语言与文档

- 对话、项目文档、代码注释、文档字符串和用户可见错误信息使用中文。
- 代码标识符保持英文；文档统一使用 Markdown。
- 修改行为或配置时，同步更新 README 或相关 `docs/` 文档。

## 项目角色

- 主 AI 始终负责需求理解、权限判断、主要实现、外部结果验收和最终交付。
- 非简单的编码、架构、疑难排查或独立复核任务，应先加载仓库技能
  `$lingshu-orchestrator`，再判断外部模型调用是否能实质提高速度或质量。
- 用户明确要求调用灵枢子代理时，在安全边界允许的前提下执行；不要因为技能存在而对简单、
  确定性的任务机械地产生真实请求。
- 常规外部模型任务优先使用方舟 Coding Plan。DeepSeek 官方和阿里云百炼仅用于用户明确的
  试用、比较或独立复核。

## 安全边界

- 不读取、打印、记录、提交或发送 `.env`、API Key、访问令牌、私钥及其他凭据。
- `.env.example` 只能包含空的凭据字段和可公开的示例值。
- 外部模型只获得完成子任务所需的最小文件范围；不要发送无关源码、个人文件、运行数据或
  供应商凭据。
- 不允许外部模型直接修改主仓库或执行任意 Shell。它只能返回分析、草稿或统一 diff，或写入
  灵枢任务产物目录。
- 只有用户明确允许真实调用，且 `LINGSHU_ENABLE_LIVE_TESTS=true` 时，才执行 `run` 或
  `doctor --live`。真实请求可能消耗套餐额度或产生费用。
- `data/`、`.git/`、`.env*`、凭据文件和依赖目录必须保持在 Git 与外部模型读取范围之外。
- DeepSeek 与百炼依赖供应商侧预算限制；本项目记录用量，但不在本机维护累计金额上限。

## 实现约束

- 保持 Provider 之间的实现边界，即使当前端点都兼容 OpenAI 协议。
- 默认使用模拟 HTTP 传输进行测试，除非用户明确启用真实测试。
- 只做与当前任务相关的改动，保留用户已有且无关的工作区修改。
- 配置、日志和错误响应不得包含完整密钥；疑似泄露时优先撤销和轮换密钥。

## 验收

后端改动至少运行：

```powershell
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\ruff.exe check .
.\.venv\Scripts\pyright.exe
```

前端改动还需运行：

```powershell
Set-Location web
npm run test
npm run build
```

外部模型的“已完成”不能替代本地验收。应用补丁前先检查内容与适用范围，应用后运行与改动
风险相称的测试和静态检查。
